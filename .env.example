# PostgreSQL connection string (required for run and migrations)
DATABASE_URL=postgresql://user:password@localhost:5432/review_db

# Optional: PostgreSQL schema (default reviewdb)
# DATABASE_SCHEMA=reviewdb

# Optional: port for local Express server (default 3000)
# PORT=3000

# Optional: CORS allowed origins (comma-separated). Default: http://localhost:3000, http://localhost:3001
# CORS_ORIGIN=http://localhost:3000,https://myapp.com

# Optional: NODE_ENV (development | production)
# NODE_ENV=development

# Supabase JWT secret for verifying Bearer tokens (Project Settings -> API -> JWT Secret)
# Required for JWT-secured review POST/PUT/DELETE when not using API Gateway authorizer
SUPABASE_JWT_SECRET=
